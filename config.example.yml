bluesky:
  # download_car.rb を使う場合に指定します。
  # 手動で CAR ファイルを取得する場合は使いません。
  handle: bsky.app

extract:
  car_path: repo.car
  out_dir: out

obsidian:
  vault_path: sample-obsidian
  timezone: Asia/Tokyo

  daily:
    path_format: "Daily/%Y/%Y-%m-%d.md"

  posts:
    exclude_texts: []
